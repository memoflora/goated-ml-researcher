# Orchestrator dependencies. The generated pipeline.py may NOT import from here —
# its whitelist is requirements-pipeline.txt (owner: C).
#
# agent.py speaks the Anthropic Messages shape and adapts the others to it, so the
# orchestrator never knows which one is live — that is what lets TECHJAM_FALLBACK_LLM
# switch providers mid-run when one starts throttling, with no code change.
#
# Gemini is deliberately absent from this list: it is two REST endpoints, reached with
# `urllib` from the standard library. We already turn every SDK's own retries off (we
# own the retry and failover policy), so a third provider SDK would buy us nothing and
# add one more thing that can fail to install on Python 3.14 the weekend it matters.
anthropic==0.51.0
openai==1.66.5
pytest==8.3.4
ruff==0.9.6

# Added by C after the first real run — both were missing and both failed silently.
# pyyaml: knowledge.py could not load ideas.yaml, so D's entire idea bank was absent
#   from every prompt and 15 tests failed.
# matplotlib: report.py's write_trajectory_png() returns False when it cannot import,
#   so trajectory.png — the headline chart — was never written and nothing said so.
# pandas: datacard.py's compute_stats() imports it to build the card from the CSVs.
matplotlib==3.10.0
pandas==2.2.3
pyyaml==6.0.2
